<!--
On the target device this file must be located in /vendor/etc and named
audio_hal_configuration.xml

Syntax is based on TinyHAL:
  https://github.com/CirrusLogic/tinyhal/blob/master/audio.example.xml

Note: not xml elements from TinyHAL are supported.
-->
<audiohal>
    <!-- mixer element _must_ appear before any of the other definitions.
    The 'card' attribute is optional and sets the ALSA card number of the mixer
    device - if not given it defaults to 0 -->
        <mixer card="0">
        <!-- init element lists control settings required to initialize the
        hardware and driver. These settings are applied only once when primary HAL
        is first loaded during boot -->
        <init>
            <!-- playback -->
            <!-- jack out -->
            <ctl name="O03 I05 Switch" val="1" />
            <ctl name="O04 I06 Switch" val="1" />
            <ctl name="INT ADDA O03_O04 Switch" val="1" />
            <ctl name="AUD_CLK_BUF_Switch" val="On"/>
            <!-- capture -->
            <!-- dmic -->
            <ctl name="O09 I14 Switch" val="1" />
            <ctl name="O10 I15 Switch" val="1" />
        </init>
        </mixer>

        <!-- jack out -->
        <device name="headphone">
            <path name="on">
                <ctl name="Audio_Amp_R_Switch" val="On"/>
                <ctl name="Audio_Amp_L_Switch" val="On"/>
            </path>
            <path name="off">
                <ctl name="Audio_Amp_R_Switch" val="Off"/>
                <ctl name="Audio_Amp_L_Switch" val="Off"/>
            </path>
        </device>

        <!-- camera in -->
        <device name="mic">
            <path name="on">
                <ctl name="Audio_MICBIAS0_Switch" val="On" />
            </path>
            <path name="off">
                <ctl name="Audio_MICBIAS0_Switch" val="Off" />
            </path>
        </device>

        <stream type="pcm" dir="out" card="0" device="0" rate="48000">
        </stream>

        <stream type="pcm" dir="in" card="0" device="3" rate="48000">
        </stream>

</audiohal>
